class Twofer {
    String twofer(String name) {
        if(name=="Alice"){
            return "One for "+name+", one for me.";
        }
        if (name=="Bob"){
            return "One for "+name+", one for me.";
        }
        else {
            return "One for you, one for me.";
        }
    }
}
