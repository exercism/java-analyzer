package analyzer.exercises.twofer;

/** Using loops deserves mentor review. */
public class Twofer {
    public String twofer(String name) {
        for (int i = 0; i < 10; i++) {
            // noop
        }
        return "One for " + name + ", one for me.";
    }
}